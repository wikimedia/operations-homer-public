{% macro section(section) %}{% include "cr/" + section + ".conf" %}{% endmacro %}
replace: groups {
    {{ section("groups")|indent(4) }}
}
replace: system {
    {{ section("system")|indent(4) }}
}
replace: logical-systems {
    {{ section("logical-systems")|indent(4) }}
}
{#
chassis {
}
#}
{% if site == "ulsfo" %}
replace: services {
    {{ section("services")|indent(4) }}
}
{% endif %}
{#
interfaces {
}
#}
replace: snmp {
    {{ section("snmp")|indent(4) }}
}
{#
forwarding-options {
}
routing-options {
}
protocols {
}
#}
replace: policy-options {
    {{ section("policy-options")|indent(4) }}
}
replace: firewall {
    {{ section("firewall")|indent(4) }}
}
