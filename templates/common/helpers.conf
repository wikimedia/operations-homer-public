{% if dhcp_relay | d(false) %}
helpers {
    bootp {
        server {{ dhcp_server }};
        interface {
            {% for interface in dhcp_relay.interfaces %}
            {{ interface }};
            {% endfor %}
        }
    }
}
{% endif %}
