{% macro section(cat, section) %}{% include cat + "/" + section + ".conf" %}{% endmacro %}
replace: system {
    {{ section("cloudsw", "system")|indent(4) }}
}
replace: snmp {
    {{ section("common", "snmp")|indent(4) }}
}
{{ section("cloudsw", "interfaces") }}
chassis {
    {{ section("common", "chassis")|indent(4) }}
}
protocols {
    replace: layer2-control {
    }
}
{{ section("common", "lldp") }}

{{ section("common", "vlans") }}

replace: routing-instances {
    {{ section("common", "routing-instances")|indent(4) }}
}

{# TODO: OSPF #}