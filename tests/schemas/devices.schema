{
  "title": "devices.yaml | Homer Public",
  "type": "object",
  "additionalProperties" : false,
  "patternProperties": {
    "^.+$": {
      "type": "object",
      "additionalProperties" : false,
      "properties": {
        "config": {
          "$ref": "definitions/device-generic.schema"
        }
      },
      "required": [
        "config"
      ]
    }
  }
}
