{
  "type": "object",
  "additionalProperties" : false,
  "patternProperties": {
    "^.+$": {
      "type": "object",
      "additionalProperties" : false,
      "properties": {
        "role": {
          "$ref": "definitions/available-roles.schema"
        },
        "site": {
          "$ref": "definitions/available-sites.schema"
        },
        "config": {
          "$ref": "definitions/device-generic.schema"
        }
      },
      "required": [
        "site",
        "role"
      ]
    }
  }
}
